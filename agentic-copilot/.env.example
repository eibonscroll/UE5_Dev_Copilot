INDEX_ON_STARTUP=auto   # auto | always | never
REINDEX_TOKEN=dev-secret     # simple guard for the reindex endpoint

OPENAI_API_KEY=
# pick a model per call; not stored in the key
OPENAI_CHAT_MODEL=gpt-4.1-mini
OPENAI_EMBED_MODEL=text-embedding-3-large
OPENAI_TEMPERATURE=0.2

# retrieval knobs
RAG_TOP_K=60                 # initial vector hits before rerank
RERANK_TOP_N=8               # how many to pass to LLM

QDRANT_URL=http://qdrant:6333
VECTOR_COLLECTION=agentic_copilot
EMBEDDING_MODEL=BAAI/bge-small-en
# WEB
NEXT_PUBLIC_API_BASE=/api